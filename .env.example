PORT=3000
DATA_FILE=data/dev-store.json

# Set this in production to use PostgreSQL instead of local JSON storage.
# Render and Supabase connection strings usually need SSL.
DATABASE_URL=
PGSSL=true

# Set true temporarily in production to inspect /debug/persistence with a logged-in account.
ENABLE_ADMIN_DEBUG=false
